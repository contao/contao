{
    "scripts": {
        "post-install-cmd": [
            "composer update --working-dir=webspace"
        ],
        "post-update-cmd": [
            "composer update --working-dir=webspace"
        ]
    }
}

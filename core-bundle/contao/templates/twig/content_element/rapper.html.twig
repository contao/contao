{% extends "@Contao/content_element/_base.html.twig" %}

{% block attributes %} style="border: 5px solid red"{% endblock %}

{% block inner %}
    <fieldset>
        <legend>Nested elements:</legend>
        {% for element in nested_elements|default([]) %}
            {{ render(element) }}
        {% endfor %}
    </fieldset>
{% endblock %}

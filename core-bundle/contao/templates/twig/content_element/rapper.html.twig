{% extends "@Contao/content_element/_base.html.twig" %}

{% block attributes %} style="border: 5px solid red"{% endblock %}

{% block inner %}
    <fieldset>
        <legend>Slot default</legend>
        {% for element in nested_elements.default|default([]) %}
            {{ render(element) }}
        {% endfor %}
    </fieldset>
{% endblock %}

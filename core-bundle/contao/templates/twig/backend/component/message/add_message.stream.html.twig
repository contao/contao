{% use "@Contao/backend/component/message/_message.html.twig" %}

{% set id = id|default('messages') %}
<turbo-stream action="append" target="{{ id }}">
    <template>
        {{ block('message_component') }}
    </template>
</turbo-stream>

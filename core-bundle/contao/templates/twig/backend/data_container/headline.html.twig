{{ knp_menu_render('be_breadcrumb_menu') }}
{#
{% if tree_trail|default %}
    <div class="tree-trail">
        <span>🌍</span>
        <ul>
            {% for trail in tree_trail|reverse %}
                <li>
                    <a{{ attrs().set('href', trail.url) }}>{{ trail.label }}</a>
                </li>
            {% endfor %}
        </ul>
    </div>
{% endif %}
<span>
    <a{{ attrs().set('href', link_url) }}>{{ link_label }}</a>
    {% if tree_siblings %}
        <ul>
            {% for sibling in tree_siblings %}
                <li>
                    <a{{ attrs().set('href', sibling.url).addClass('tl_label', sibling.active|default) }}>
                        {{ sibling.label }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    {% endif %}
</span>
#}

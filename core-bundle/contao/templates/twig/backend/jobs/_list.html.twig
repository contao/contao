{% trans_default_domain 'contao_jobs' %}

{% macro render_jobs(jobs) %}
    <ul>
        {% for job in jobs %}
            <li>
                {{ ('jobs.typeLabel.' ~ job.type)|trans }} ({{ job.status.label|trans }} - {{ job.createdAt|date(contao.datim_format) }})
            </li>
        {% endfor %}
    </ul>
{% endmacro %}

{% block main %}
    <div{{ list_attributes|default }}>
        {{ _self.render_jobs(jobs) }}
    </div>
{% endblock %}

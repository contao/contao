{#
    This component outputs a script tag.
    It can have an optional content or a source via attributes.
    It will automatically add a nonce if applicable.

        <script>…</script>
        <script nonce="…">…</script>
        <script nonce="…" src="…"></script>

    Optional variables:
        @var \Contao\CoreBundle\String\HtmlAttributes attributes
#}

{% block script_component %}
    {% set script_attributes = attrs(script_attributes|default)
        .setIfExists('nonce', contao_csp_nonce('script-src'))
    %}

    <script{{ script_attributes }}>
        {% block script_content %}
            {# Output inline scripts here. #}
        {% endblock %}
    </script>
{% endblock %}

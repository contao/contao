{% for reference in references %}
    {% block element %}
        {{ reference.type == 'content_element' ? content_element(reference.id) : frontend_module(reference.id) }}
    {% endblock %}
{% endfor %}

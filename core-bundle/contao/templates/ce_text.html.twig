{% extends '@Contao/block_searchable.html.twig' %}

{% block content %}

    {% if not addBefore %}
        {{ text|default|sanitize_html('contao')|csp_inline_styles|insert_tag_raw }}
    {% endif %}

    {% if addImage %}
        {{ include('@Contao/image.html.twig') }}
    {% endif %}

    {% if addBefore %}
        {{ text|default|sanitize_html('contao')|csp_inline_styles|insert_tag_raw }}
    {% endif %}

{% endblock %}

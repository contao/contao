[data-controller="contao--password-visibility"] {
    position: relative;

    > input {
        padding-right: 28px;
    }

    > label {

    }

    > button {
        position: absolute;
        inset: 3px 0 3px auto;
        padding: 4px 8px 4px 4px;
        border: 0;
        background: transparent;
    }
}

services:
    _defaults:
        autoconfigure: true
        public: true

    Contao\CoreBundle\Controller\Page\RootPageController:
        arguments:
            - '@database_connection'
        tags:
            - { name: contao.page }
            - { name: monolog.logger, channel: contao }

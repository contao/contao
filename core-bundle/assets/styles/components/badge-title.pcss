.badge-title {
    float: right;
    margin-left: 8px;
    margin-top: -8px;
    border-radius: 12px;
    padding: 1px 5px 2px;
    background: var(--invert-bg);
    color: var(--invert-text);
    font-size: 0.75rem;
    font-weight: 600;
    line-height: normal;
}

mcp:
    servers:
        contao_backend:
            name: Contao Backend
            transports:
                http: true
                stdio: false
            http:
                path: '%contao_mcp.backend_path%'
            session:
                store: file
                directory: '%kernel.cache_dir%/mcp/contao_backend'
                ttl: 3600

mcp:
    client_transports:
        http: true
        stdio: false
    http:
        path: '%contao_mcp.path%'
        session:
            store: file
            directory: '%kernel.cache_dir%/mcp'
            ttl: 3600

contao_mcp_backend:
    path: '%contao.backend.route_prefix%/_mcp'
    controller: mcp.server.contao_backend.controller::handle
    methods: [GET, POST, DELETE, OPTIONS]
    defaults:
        _scope: backend

{% trans_default_domain 'contao_default' %}
{% extends '@Contao/block_unsearchable.html.twig' %}

{% block content %}

    {% if not articles %}
        <p class="empty">{{ 'MSC.empty'|trans }}</p>
    {% else %}
        {{ articles|join|raw }}
        {{ pagination|default|raw }}
    {% endif %}

{% endblock %}
